<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_add_note"
        android:title="@string/menu_add_note"
        android:icon="@android:drawable/ic_menu_add"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_delete_note"
        android:title="@string/menu_delete_note"
        android:icon="@android:drawable/ic_menu_delete"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_switch_storage"
        android:title="@string/menu_switch_storage"
        android:icon="@android:drawable/ic_menu_save"
        app:showAsAction="never" />

</menu>